import { Box } from "@mui/material"

const PageNotFound = () => {
    return (
        <Box sx={{ p: 2 }}>
            Hello! Seems like this page doesn't exist yet, but stay tuned!
        </Box>
    )
}

export default PageNotFound